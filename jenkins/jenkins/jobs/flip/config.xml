<?xml version='1.1' encoding='UTF-8'?>
<project>
  <actions/>
  <description></description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <com.coravy.hudson.plugins.github.GithubProjectProperty plugin="github@1.29.5">
      <projectUrl>https://github.com/$GIT_ROOT/Cars_Sample_App/</projectUrl>
      <displayName></displayName>
    </com.coravy.hudson.plugins.github.GithubProjectProperty>
  </properties>
  <scm class="hudson.plugins.git.GitSCM" plugin="git@4.1.1">
    <configVersion>2</configVersion>
    <userRemoteConfigs>
      <hudson.plugins.git.UserRemoteConfig>
        <url>git@github.com:$GIT_ROOT/Cars_Sample_App.git</url>
        <credentialsId>544a1f7f-fe5c-473e-bcb5-6d7fd9ebbf96</credentialsId>
      </hudson.plugins.git.UserRemoteConfig>
    </userRemoteConfigs>
    <branches>
      <hudson.plugins.git.BranchSpec>
        <name>*/master</name>
      </hudson.plugins.git.BranchSpec>
    </branches>
    <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
    <submoduleCfg class="list"/>
    <extensions/>
  </scm>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers>
    <hudson.triggers.TimerTrigger>
      <spec>0 0-21/3 * * *
30 1-22/3 * * *</spec>
    </hudson.triggers.TimerTrigger>
  </triggers>
  <concurrentBuild>false</concurrentBuild>
  <builders>
    <hudson.tasks.Shell>
      <command># Add SSH key from Volume Mount
cp /keys/id_rsa /id_rsa
sed -i -e '$a\' /id_rsa
chmod 600 /id_rsa
eval "$(ssh-agent -s)"
ssh-add /id_rsa

cd daily_tasks
echo $PWD
ls -ltrh
# Send in ref data
curl -s -v -H 'Content-Type: application/json' -X POST $PROXY_ENDPOINT/v1/observations/httpscodes -d @HTTPCodes.json
curl -s -v -H 'Content-Type: text/csv' -X POST $PROXY_ENDPOINT/v1/observations/users --data-binary @users.csv
curl -s -v -H 'Content-Type: text/csv' -X POST $PROXY_ENDPOINT/v1/observations/companies --data-binary @companies.csv

# Make Git Commit
git config --global user.email tom.batchelor@me.com
git config --global user.name tombatchelor
cd ../app_server
if  `grep -q 'TRUE' Dockerfile`; then
  sed -i 's/TRUE/FALSE/g' Dockerfile
  git add Dockerfile
  git commit -m "Resolving mem leak issue"
  IS_FAILING=FALSE
else
  sed -i 's/FALSE/TRUE/g' Dockerfile
  git add Dockerfile
  git commit -m "Small change to cache code"
  IS_FAILING=TRUE
fi
git push origin HEAD:master
</command>
    </hudson.tasks.Shell>
  </builders>
  <publishers/>
  <buildWrappers/>
</project>